How each metric is generated (from engine.py:516-651):

Metric	Value	How it's computed
Energy risk	0.989	SLT hidden states → energy probe → predict_proba → 1 - confidence. Measures if internal state resembles incorrect answers.
Entropy risk	0.554	SLT hidden states → entropy probe → predict_proba directly. Measures internal uncertainty.
Combined risk	0.303	For short answers (≤100 tokens): (energy + entropy) / 2. For long answers: weighted blend of SLT probe + per-sentence probe risks.
Sentence avg conf	91.5%	Mean of per-sentence logit-based confidence scores (from B1 baseline — raw token logits through sigmoid).
Confidence badge	69.7% HIGH	1 - combined_risk displayed as percentage. Thresholds: <0.35 = HIGH, 0.35-0.65 = MEDIUM, ≥0.65 = LOW.
The problem in your screenshot: Energy risk is 0.989 (extremely high risk) but combined risk is only 0.303 — that's mathematically impossible with (0.989 + 0.554) / 2 = 0.77. This means the combined risk shown is actually coming from the TBG probe (pre-generation phase 1), not the SLT probe. Looking at the code:

Read d:\Github Repositories\SemanticEnergy\frontend\script.js (lines 671-690)
There it is — the metrics panel is using tbgData (TBG probe scores) but labelling them generically. The energy/entropy/combined risk shown in the Details panel are all from the TBG pre-generation probe, NOT the SLT post-generation probe. The SLT data is only used for sentence scores and sentence avg confidence.

This is confusing because the user sees "Fast SLT" mode but the detail metrics are TBG values. The SLT probe's own energy/entropy/combined risk values are computed but never shown in the panel.

Here's what I recommend:

Show both TBG and SLT metrics clearly separated in the Details panel, so users can see pre-generation vs post-generation risk
Update the guide modal with accurate formulas
Add a hallucination score history chart that tracks combined_risk over questions in the session


All four metrics serve different purposes — I'd recommend keeping all of them but you may want to consider:

Energy Risk + Entropy Risk are the raw probe outputs. They're useful for diagnostics but can be confusing when they diverge from Combined Risk (as in your screenshot where energy=0.989 but combined=0.303 due to the long-answer blending formula). You could consider putting them under an expandable "Probe Breakdown" sub-section if you want a cleaner default view.
Combined Risk is the one that matters — it drives the confidence badge
Sentence Avg Conf is a lightweight supplementary signal — useful for catching "confidently wrong" cases